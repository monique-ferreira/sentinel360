# ─── Sentinel360 Backend – requirements.txt ───────────────────────────────────
# Instale com: pip install -r requirements.txt

# API Framework
fastapi==0.115.0
uvicorn[standard]==0.30.6

# Banco de dados
motor==3.5.1          # MongoDB async
pymongo==4.8.0
bson==0.5.10

# Autenticação
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.0

# HTTP Client (Office 365 + notificações)
httpx==0.27.2

# Validação
pydantic[email]==2.9.2
email-validator==2.2.0

# Variáveis de ambiente
python-dotenv==1.0.1

# Agendamento (opcional – para scans periódicos no servidor)
# apscheduler==3.10.4

# ─── Variáveis de ambiente necessárias (.env) ─────────────────────────────────
# MONGO_URL=mongodb+srv://user:pass@cluster.mongodb.net/
# DB_NAME=sentinel360
# SECRET_KEY=gere-com-python-secrets-token-hex-32
# ANTHROPIC_API_KEY=sk-ant-...            (opcional – ativa detecção por IA)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=seu@email.com
# SMTP_PASS=app-password
# ALLOWED_ORIGINS=https://sentinel360-cyber.vercel.app,http://localhost:5173
